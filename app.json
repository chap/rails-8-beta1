{
    "buildpacks": [
        { "url": "heroku/ruby" }
      ],
    "environments": {
      "test": {
        "scripts": {
          "test": "rails test"
        }
        // "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"]
      }
    }
  }